{#
  This file is auto-generated.

  Track - The track element is used as a child of the media elements—audio and video. It lets you specify timed text tracks (or time-based data), for example to automatically handle subtitles. The tracks are formatted in WebVTT format (.vtt files) — Web Video Text Tracks.

  Symfony UX Twig Component (Anonymous)
  @see https://symfony.com/bundles/ux-twig-component/current/index.html

  Usage:
    <twig:Track class="custom-class" id="my-track" />

  @author vardumper <info@erikpoehler.com>
  @package vardumper/extended-htmldocument
  @see src/TemplateGenerator/TwigComponentsGenerator.php
#}
{% apply spaceless %}
<track
  {% if this.class is defined and this.class is not null %}class="{{ this.class }}"{% endif %}
  {% if this.default is defined and this.default is not null %}default="{{ this.default }}"{% endif %}
  {% if this.hidden is defined and this.hidden is not null %}hidden="{{ this.hidden }}"{% endif %}
  {% if this.id is defined and this.id is not null %}id="{{ this.id }}"{% endif %}
  {% if this.kind is not null %}kind="{{ this.kind.value }}"{% endif %}
  {% if this.label is defined and this.label is not null %}label="{{ this.label }}"{% endif %}
  {% if this.lang is defined and this.lang is not null %}lang="{{ this.lang }}"{% endif %}
  {% if this.src is defined and this.src is not null %}src="{{ this.src }}"{% endif %}
  {% if this.srclang is defined and this.srclang is not null %}srclang="{{ this.srclang }}"{% endif %}
  {% if this.style is defined and this.style is not null %}style="{{ this.style }}"{% endif %}
/>
