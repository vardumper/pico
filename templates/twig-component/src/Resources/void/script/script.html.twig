{#
  This file is auto-generated.

  Script - The script element is used to embed or reference an executable script within an HTML document. Scripts without async or defer attributes, as well as inline scripts, are fetched and executed immediately, before the browser continues to parse the page.

  Symfony UX Twig Component (Anonymous)
  @see https://symfony.com/bundles/ux-twig-component/current/index.html

  Usage:
    <twig:Script class="custom-class" crossorigin="anonymous">
      Content goes here
    </twig:Script>

  @author vardumper <info@erikpoehler.com>
  @package vardumper/extended-htmldocument
  @see src/TemplateGenerator/TwigComponentsGenerator.php
#}
{% apply spaceless %}
<script
  {% if this.async is defined and this.async is not null %}async="{{ this.async }}"{% endif %}
  {% if this.charset is defined and this.charset is not null %}charset="{{ this.charset }}"{% endif %}
  {% if this.class is defined and this.class is not null %}class="{{ this.class }}"{% endif %}
  {% if this.crossorigin is not null %}crossorigin="{{ this.crossorigin.value }}"{% endif %}
  {% if this.defer is defined and this.defer is not null %}defer="{{ this.defer }}"{% endif %}
  {% if this.hidden is defined and this.hidden is not null %}hidden="{{ this.hidden }}"{% endif %}
  {% if this.id is defined and this.id is not null %}id="{{ this.id }}"{% endif %}
  {% if this.integrity is defined and this.integrity is not null %}integrity="{{ this.integrity }}"{% endif %}
  {% if this.lang is defined and this.lang is not null %}lang="{{ this.lang }}"{% endif %}
  {% if this.nonce is defined and this.nonce is not null %}nonce="{{ this.nonce }}"{% endif %}
  {% if this.referrerpolicy is not null %}referrerpolicy="{{ this.referrerpolicy.value }}"{% endif %}
  {% if this.src is defined and this.src is not null %}src="{{ this.src }}"{% endif %}
  {% if this.title is defined and this.title is not null %}title="{{ this.title }}"{% endif %}
  {% if this.type is not null %}type="{{ this.type.value }}"{% endif %}
>
  {%- block content %}{% endblock -%}
</script>
{% endapply %}
