{#
  This file is auto-generated.

  Form - The form element represents a section of a document containing interactive controls for submitting information to a web server.

  @author vardumper <info@erikpoehler.com>
  @package vardumper/extended-htmldocument
  @see src/TemplateGenerator/TwigGenerator.php
#}
{% block form %}
{% set ariaInvalid_choices = {'false': true, 'true': true, 'grammar': true, 'spelling': true} %}
{% set ariaLabel_choices = {'breadcrumb': true} %}
{% set autocomplete_choices = {'off': true, 'on': true} %}
{% set autocorrect_choices = {'off': true, 'on': true} %}
{% set dataPlacement_choices = {'top': true, 'left': true, 'right': true, 'bottom': true} %}
{% set dataTheme_choices = {'light': true, 'dark': true} %}
{% set dir_choices = {'ltr': true, 'rtl': true, 'auto': true} %}
{% set enctype_choices = {'application/x-www-form-urlencoded': true, 'multipart/form-data': true, 'text/plain': true} %}
{% set method_choices = {'get': true, 'post': true} %}
{% set target_choices = {'_blank': true, '_parent': true, '_self': true, '_top': true} %}
{% set translate_choices = {'yes': true, 'no': true} %}
<form
  {% if acceptCharset is defined and acceptCharset|length > 0 %}accept-charset="{{ acceptCharset }}"{% endif %}
  {% if accesskey is defined and accesskey|length > 0 %}accesskey="{{ accesskey }}"{% endif %}
  {% if action is defined and action|length > 0 %}action="{{ action }}"{% endif %}
  {% if ariaInvalid is defined and ariaInvalid|length > 0 and ariaInvalid_choices[ariaInvalid] is defined %}aria-invalid="{{ ariaInvalid }}"{% endif %}
  {% if ariaLabel is defined and ariaLabel|length > 0 and ariaLabel_choices[ariaLabel] is defined %}aria-label="{{ ariaLabel }}"{% endif %}
  {% if autocomplete is defined and autocomplete|length > 0 and autocomplete_choices[autocomplete] is defined %}autocomplete="{{ autocomplete }}"{% endif %}
  {% if autocorrect is defined and autocorrect|length > 0 and autocorrect_choices[autocorrect] is defined %}autocorrect="{{ autocorrect }}"{% endif %}
  {% if class is defined and class|length > 0 %}class="{{ class }}"{% endif %}
  {% if dataPlacement is defined and dataPlacement|length > 0 and dataPlacement_choices[dataPlacement] is defined %}data-placement="{{ dataPlacement }}"{% endif %}
  {% if dataTheme is defined and dataTheme|length > 0 and dataTheme_choices[dataTheme] is defined %}data-theme="{{ dataTheme }}"{% endif %}
  {% if dataTooltip is defined and dataTooltip|length > 0 %}data-tooltip="{{ dataTooltip }}"{% endif %}
  {% if dir is defined and dir|length > 0 and dir_choices[dir] is defined %}dir="{{ dir }}"{% endif %}
  {% if draggable is defined and draggable|length > 0 %}draggable="{{ draggable }}"{% endif %}
  {% if enctype is defined and enctype|length > 0 and enctype_choices[enctype] is defined %}enctype="{{ enctype }}"{% endif %}
  {% if hidden is defined and hidden|length > 0 %}hidden="{{ hidden }}"{% endif %}
  {% if id is defined and id|length > 0 %}id="{{ id }}"{% endif %}
  {% if lang is defined and lang|length > 0 %}lang="{{ lang }}"{% endif %}
  {% if method is defined and method|length > 0 and method_choices[method] is defined %}method="{{ method }}"{% endif %}
  {% if name is defined and name|length > 0 %}name="{{ name }}"{% endif %}
  {% if novalidate is defined and novalidate|length > 0 %}novalidate="{{ novalidate }}"{% endif %}
  {% if slot is defined and slot|length > 0 %}slot="{{ slot }}"{% endif %}
  {% if style is defined and style|length > 0 %}style="{{ style }}"{% endif %}
  {% if tabindex is defined and tabindex|length > 0 %}tabindex="{{ tabindex }}"{% endif %}
  {% if target is defined and target|length > 0 and target_choices[target] is defined %}target="{{ target }}"{% endif %}
  {% if title is defined and title|length > 0 %}title="{{ title }}"{% endif %}
  {% if translate is defined and translate|length > 0 and translate_choices[translate] is defined %}translate="{{ translate }}"{% endif %}>
  {% block content %}{{- content|raw -}}{% endblock %}
</form>
{% endblock %}
