/**
 * THIS FILE IS AUTOGENERATED. DO NOT EDIT IT.
 *
 * @generated November 7, 2025 17:23:48
 * @component Area
 * @description The area element represents either a hyperlink with some text and a corresponding area on an image map, or a dead area on an image map.
 */

import React from 'react';

export interface AreaProps {
  /** CSS class names */
  className?: string;
  accesskey?: string;
  /** Specifies alternative text to be displayed when the image cannot be rendered. */
  alt: string;
  autocapitalize?: 'none' | 'sentences' | 'words' | 'characters';
  autofocus?: boolean;
  contenteditable?: 'true' | 'false' | 'inherit';
  /** Specifies the coordinates of the shape in a rectangular area or a polygonal area on an image map. */
  coords?: string;
  /** Custom data attributes (data-*) */
  data?: Record<string, string>;
  dir?: 'ltr' | 'rtl' | 'auto';
  /** Indicates that the linked content should be downloaded rather than displayed. */
  download?: string;
  draggable?: boolean;
  hidden?: boolean;
  /** Specifies the URL of the linked resource. Special protocols such as mailto: or tel: can be used. */
  href: string;
  /** Specifies the language of the linked resource. */
  hreflang?: string;
  id?: string;
  inputmode?: 'none' | 'text' | 'decimal' | 'numeric' | 'email' | 'tel' | 'url' | 'search';
  lang?: string;
  /** Specifies the relationship between the current document and the linked document. */
  rel?: 'alternate' | 'author' | 'bookmark' | 'canonical' | 'help' | 'icon' | 'license' | 'next' | 'nofollow' | 'noreferrer' | 'prefetch' | 'prev' | 'search' | 'stylesheet' | 'tag';
  shape?: 'circle' | 'default' | 'poly' | 'rect';
  spellcheck?: 'true' | 'false';
  style?: React.CSSProperties;
  tabindex?: number;
  /** Specifies where to open the linked document. */
  target?: '_blank' | '_parent' | '_self' | '_top';
  title?: string;
  translate?: 'yes' | 'no';
  /** Specifies the media type of the linked resource. */
  type?: string;
}

/**
 * Area - The area element represents either a hyperlink with some text and a corresponding area on an image map, or a dead area on an image map.
 */
export const Area: React.FC<AreaProps> = (props: AreaProps) => {
  const {
    className,
    accesskey,
    alt,
    autocapitalize,
    autofocus,
    contenteditable,
    coords,
    data,
    dir,
    download,
    draggable,
    hidden,
    href,
    hreflang,
    id,
    inputmode,
    lang,
    rel,
    shape,
    spellcheck,
    style,
    tabindex,
    target,
    title,
    translate,
    type
  } = props;

  const elementProps: React.HTMLAttributes<HTMLElement> & Record<string, any> = {};

  if (className) {
    elementProps.className = className;
  }

  if (data) {
    Object.entries(data).forEach(([key, value]) => {
      elementProps[`data-${key}`] = value;
    });
  }

  if (accesskey !== undefined) {
    elementProps['accesskey'] = accesskey;
  }

  if (alt !== undefined) {
    elementProps['alt'] = alt;
  }

  if (autocapitalize !== undefined) {
    elementProps['autocapitalize'] = autocapitalize;
  }

  if (autofocus !== undefined) {
    elementProps['autofocus'] = autofocus;
  }

  if (contenteditable !== undefined) {
    elementProps['contenteditable'] = contenteditable;
  }

  if (coords !== undefined) {
    elementProps['coords'] = coords;
  }

  if (dir !== undefined) {
    elementProps['dir'] = dir;
  }

  if (download !== undefined) {
    elementProps['download'] = download;
  }

  if (draggable !== undefined) {
    elementProps['draggable'] = draggable;
  }

  if (hidden !== undefined) {
    elementProps['hidden'] = hidden;
  }

  if (href !== undefined) {
    elementProps['href'] = href;
  }

  if (hreflang !== undefined) {
    elementProps['hreflang'] = hreflang;
  }

  if (id !== undefined) {
    elementProps['id'] = id;
  }

  if (inputmode !== undefined) {
    elementProps['inputmode'] = inputmode;
  }

  if (lang !== undefined) {
    elementProps['lang'] = lang;
  }

  if (rel !== undefined) {
    elementProps['rel'] = rel;
  }

  if (shape !== undefined) {
    elementProps['shape'] = shape;
  }

  if (spellcheck !== undefined) {
    elementProps['spellcheck'] = spellcheck;
  }

  if (style !== undefined) {
    elementProps['style'] = style;
  }

  if (tabindex !== undefined) {
    elementProps['tabindex'] = tabindex;
  }

  if (target !== undefined) {
    elementProps['target'] = target;
  }

  if (title !== undefined) {
    elementProps['title'] = title;
  }

  if (translate !== undefined) {
    elementProps['translate'] = translate;
  }

  if (type !== undefined) {
    elementProps['type'] = type;
  }

  return React.createElement(
    'area',
    elementProps
  );
};

Area.displayName = 'Area';
