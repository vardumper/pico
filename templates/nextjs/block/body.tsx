/**
 * THIS FILE IS AUTOGENERATED. DO NOT EDIT IT.
 *
 * @generated November 7, 2025 17:23:46
 * @component Body
 * @description The body element represents the content of an HTML document. All the contents such as text, images, headings, links, tables, etc. are placed between the body tags.
 */

import React from 'react';

export interface BodyProps {
  /** The content to display in the element */
  children?: React.ReactNode;
  /** CSS class names */
  className?: string;
  accesskey?: string;
  /** Custom data attributes (data-*) */
  data?: Record<string, string>;
  dataPlacement?: 'top' | 'left' | 'right' | 'bottom';
  dataTheme?: 'light' | 'dark';
  dataTooltip?: string;
  dir?: 'ltr' | 'rtl' | 'auto';
  draggable?: boolean;
  hidden?: boolean;
  id?: string;
  lang?: string;
  /** Fires after the associated document has started printing or the print preview has been closed. */
  onafterprint?: string;
  /** Fires before the associated document is printed or previewed for printing. */
  onbeforeprint?: string;
  /** Fires before the user navigates away from the page. */
  onbeforeunload?: string;
  /** Fires when the fragment identifier part of the URL changes. */
  onhashchange?: string;
  /** Fires when the user changes the preferred language of the user interface. */
  onlanguagechange?: string;
  /** Fires when a message is received from a different browsing context (e.g., an iframe). */
  onmessage?: string;
  /** Fires when an error occurs while receiving a message from a different browsing context. */
  onmessageerror?: string;
  /** Fires when the browser goes offline. */
  onoffline?: string;
  /** Fires when the browser goes online. */
  ononline?: string;
  /** Fires when the user navigates away from a page. */
  onpagehide?: string;
  /** Fires when the user navigates to a page. */
  onpageshow?: string;
  /** Fires when the user navigates through the history by clicking the browser's Back or Forward buttons. */
  onpopstate?: string;
  /** Fires when a Promise is rejected and the rejection is handled by a Promise handler (e.g., catch). */
  onrejectionhandled?: string;
  /** Fires when a storage area (e.g., localStorage or sessionStorage) changes. */
  onstorage?: string;
  /** Fires when a Promise is rejected but there is no rejection handler (e.g., catch). */
  onunhandledrejection?: string;
  /** Fires when the user is navigating away from the page (similar to onbeforeunload). */
  onunload?: string;
  style?: React.CSSProperties;
  tabindex?: number;
  title?: string;
  translate?: 'yes' | 'no';
}

/**
 * Body - The body element represents the content of an HTML document. All the contents such as text, images, headings, links, tables, etc. are placed between the body tags.
 */
export const Body: React.FC<BodyProps> = (props: BodyProps) => {
  const {
    children,
    className,
    accesskey,
    data,
    dataPlacement,
    dataTheme,
    dataTooltip,
    dir,
    draggable,
    hidden,
    id,
    lang,
    onafterprint,
    onbeforeprint,
    onbeforeunload,
    onhashchange,
    onlanguagechange,
    onmessage,
    onmessageerror,
    onoffline,
    ononline,
    onpagehide,
    onpageshow,
    onpopstate,
    onrejectionhandled,
    onstorage,
    onunhandledrejection,
    onunload,
    style,
    tabindex,
    title,
    translate
  } = props;

  const elementProps: React.HTMLAttributes<HTMLElement> & Record<string, any> = {};

  if (className) {
    elementProps.className = className;
  }

  if (data) {
    Object.entries(data).forEach(([key, value]) => {
      elementProps[`data-${key}`] = value;
    });
  }

  if (accesskey !== undefined) {
    elementProps['accesskey'] = accesskey;
  }

  if (dataPlacement !== undefined) {
    elementProps['data-placement'] = dataPlacement;
  }

  if (dataTheme !== undefined) {
    elementProps['data-theme'] = dataTheme;
  }

  if (dataTooltip !== undefined) {
    elementProps['data-tooltip'] = dataTooltip;
  }

  if (dir !== undefined) {
    elementProps['dir'] = dir;
  }

  if (draggable !== undefined) {
    elementProps['draggable'] = draggable;
  }

  if (hidden !== undefined) {
    elementProps['hidden'] = hidden;
  }

  if (id !== undefined) {
    elementProps['id'] = id;
  }

  if (lang !== undefined) {
    elementProps['lang'] = lang;
  }

  if (onafterprint !== undefined) {
    elementProps['onafterprint'] = onafterprint;
  }

  if (onbeforeprint !== undefined) {
    elementProps['onbeforeprint'] = onbeforeprint;
  }

  if (onbeforeunload !== undefined) {
    elementProps['onbeforeunload'] = onbeforeunload;
  }

  if (onhashchange !== undefined) {
    elementProps['onhashchange'] = onhashchange;
  }

  if (onlanguagechange !== undefined) {
    elementProps['onlanguagechange'] = onlanguagechange;
  }

  if (onmessage !== undefined) {
    elementProps['onmessage'] = onmessage;
  }

  if (onmessageerror !== undefined) {
    elementProps['onmessageerror'] = onmessageerror;
  }

  if (onoffline !== undefined) {
    elementProps['onoffline'] = onoffline;
  }

  if (ononline !== undefined) {
    elementProps['ononline'] = ononline;
  }

  if (onpagehide !== undefined) {
    elementProps['onpagehide'] = onpagehide;
  }

  if (onpageshow !== undefined) {
    elementProps['onpageshow'] = onpageshow;
  }

  if (onpopstate !== undefined) {
    elementProps['onpopstate'] = onpopstate;
  }

  if (onrejectionhandled !== undefined) {
    elementProps['onrejectionhandled'] = onrejectionhandled;
  }

  if (onstorage !== undefined) {
    elementProps['onstorage'] = onstorage;
  }

  if (onunhandledrejection !== undefined) {
    elementProps['onunhandledrejection'] = onunhandledrejection;
  }

  if (onunload !== undefined) {
    elementProps['onunload'] = onunload;
  }

  if (style !== undefined) {
    elementProps['style'] = style;
  }

  if (tabindex !== undefined) {
    elementProps['tabindex'] = tabindex;
  }

  if (title !== undefined) {
    elementProps['title'] = title;
  }

  if (translate !== undefined) {
    elementProps['translate'] = translate;
  }

  return React.createElement(
    'body',
    elementProps,
    children
  );
};

Body.displayName = 'Body';
